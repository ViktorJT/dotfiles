export PATH="$HOME/bin:$PATH"

# Enable modern Zsh features
autoload -U compinit && compinit
autoload -U promptinit && promptinit

# Aliases
alias ll='ls -la'

echo "Loaded minimal Zsh configuration"
